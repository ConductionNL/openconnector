{
  "$comment": "ADR-037 register fragment (environments-and-promotion). Declares two schemas: `environment` (named target/source connectivity metadata, wraps an existing `source` object per design.md Decision 1/2 — no new credential or connection format) and `promotion_audit` (append-only/immutable log schema, matching call_log/job_log's x-openregister-archival convention). Also seeds two `environment` rows (`local`, `acceptance`) plus their connectivity `source` rows via `components.objects` + `@ref:<schema>:<slug>` tokens — the SAME mechanism OpenRegister's ImportHandler::resolveSeedReferenceTokens()/importFromJson() object-import loop already processes for every other OpenConnector register fragment's seed rows (verified live against OpenRegister HEAD: lib/Service/Configuration/ImportHandler.php lines ~1946-2030 processes `components.objects` with `@ref:` token resolution INSIDE importFromJson(), called from importFromApp(), which InitializeRegister.php invokes on every occ app:enable/upgrade). Deliberately NOT using `lib/environments.seed.json` (tasks.md's literal suggestion, 'following lib/sources.seed.json convention') or a schema-embedded `x-openregister-seed` array (the convention hitl-approval-rule-action.json used): neither is consumed by any code path in this app or in OpenRegister — `lib/sources.seed.json` has zero PHP references anywhere in lib/, and `x-openregister-seed` is only preserved as a round-trippable Schema config key (lib/Db/Schema.php RESERVED_KEYS) with no importer reading it back out. Following either literal precedent would have shipped a second, orphaned seed file (the exact 'spec-says-done, feature-never-runs' defect class this fleet actively guards against) instead of materialising the two seed environments design.md's Seed Data section requires.",
  "components": {
    "registers": {
      "integriq": {
        "schemas": ["environment", "promotion_audit"]
      }
    },
    "schemas": {
      "environment": {
        "slug": "environment",
        "title": "Environment",
        "icon": "ServerNetwork",
        "version": "1.0.0",
        "summary": "A named OpenConnector environment (e.g. staging, production) reachable via an existing Source",
        "description": "Named environment metadata used by the promotion workflow (environments-and-promotion). Carries NO credential or connection material of its own: `sourceRef` points at an existing `source`-schema object (type: api) whose `location` and `configuration.authentication.credentialRef` describe how to reach that environment's OpenConnector API, so dispatching a call to an environment reuses CallService::call() and BrokeredCallService's existing broker resolution unchanged. See openspec/specs/environments-and-promotion/spec.md#requirement-named-environments-are-openregister-objects-that-wrap-an-existing-source-for-connectivity-req-001.",
        "required": ["name", "slug", "sourceRef"],
        "type": "object",
        "properties": {
          "uuid": {
            "type": "string",
            "description": "Canonical UUID assigned by OpenRegister",
            "title": "UUID"
          },
          "name": {
            "type": "string",
            "description": "Human-readable environment name (e.g. \"Production\")",
            "title": "Name"
          },
          "slug": {
            "type": "string",
            "description": "Machine-readable, unique identifier used to address the environment in the promotion API (e.g. \"production\")",
            "title": "Slug"
          },
          "role": {
            "type": "string",
            "enum": ["source", "target", "both"],
            "default": "both",
            "description": "UI-filtering hint only (design.md Open Questions): whether this environment is typically promoted FROM, promoted TO, or both. Not enforced at promotion time: any environment may technically be used as source or target, matching how Sources aren't role-locked today either.",
            "title": "Role"
          },
          "sourceRef": {
            "type": "string",
            "format": "uuid",
            "$ref": "#/components/schemas/source",
            "x-openregister-onDelete": "SET_NULL",
            "description": "UUID of an existing `source`-schema object (type: api) describing how to reach this environment's OpenConnector API. Location + optional configuration.authentication.credentialRef. SET_NULL (not CASCADE) so a deleted Source leaves the environment row intact but unusable as a promotion target (REQ-001 scenario 2: rejected with an actionable error naming the missing sourceRef, not a cascaded delete).",
            "title": "Connectivity Source"
          },
          "description": {
            "type": "string",
            "description": "Optional free-text description of the environment",
            "title": "Description"
          },
          "created": {
            "type": "string",
            "format": "date-time",
            "description": "OR-managed creation timestamp",
            "title": "Created At"
          },
          "updated": {
            "type": "string",
            "format": "date-time",
            "description": "OR-managed update timestamp",
            "title": "Updated At"
          }
        },
        "x-openregister-seed": [],
        "appendOnly": false,
        "immutable": false
      },
      "promotion_audit": {
        "slug": "promotion_audit",
        "title": "Promotion Audit",
        "icon": "RocketLaunchOutline",
        "version": "1.0.0",
        "summary": "Append-only audit record of one promotion attempt (success or failure)",
        "description": "One row per confirmed promotion attempt (REQ-006). Records who promoted, what configuration, from which environment to which, when, the outcome, a counts-only preview summary (never entity payloads or credential values), how many credentialRef rebindings were applied, and the CallLog id the underlying CallService::call() dispatch produced (Decision 2/4 cross-reference). appendOnly/immutable, matching call_log/job_log. See openspec/specs/environments-and-promotion/spec.md#requirement-every-promotion-attempt-is-recorded-in-an-append-only-promotion-audit-log-req-006.",
        "required": ["configurationId", "fromEnvironmentSlug", "toEnvironmentSlug", "outcome"],
        "type": "object",
        "properties": {
          "uuid": {
            "type": "string",
            "description": "Canonical UUID assigned by OpenRegister",
            "title": "UUID"
          },
          "actorUid": {
            "type": "string",
            "description": "Nextcloud user id of the operator who confirmed the promotion",
            "title": "Actor"
          },
          "configurationId": {
            "type": "string",
            "description": "The OpenRegister configuration group id that was promoted",
            "title": "Configuration ID"
          },
          "fromEnvironmentSlug": {
            "type": "string",
            "description": "Slug of the source environment (always the local instance)",
            "title": "From Environment"
          },
          "toEnvironmentSlug": {
            "type": "string",
            "description": "Slug of the target environment",
            "title": "To Environment"
          },
          "startedAt": {
            "type": "string",
            "format": "date-time",
            "description": "Timestamp the promotion attempt started",
            "title": "Started At"
          },
          "completedAt": {
            "type": "string",
            "format": "date-time",
            "description": "Timestamp the promotion attempt completed (success or failure)",
            "title": "Completed At"
          },
          "outcome": {
            "type": "string",
            "enum": ["success", "failed", "rejected"],
            "description": "Result of the promotion attempt",
            "title": "Outcome"
          },
          "message": {
            "type": "string",
            "description": "Actionable failure detail when outcome is failed/rejected; empty on success",
            "title": "Message"
          },
          "previewSummary": {
            "type": "object",
            "description": "Counts and slugs only (never entity payloads or credential values): number of creates/updates/collisions and the entity slugs written, mirroring the target's own preview classification at the time of promotion",
            "title": "Preview Summary"
          },
          "credentialRebindCount": {
            "type": "integer",
            "default": 0,
            "description": "Number of credentialRef placeholders that were rewritten via operator-supplied credentialBindings for this promotion",
            "title": "Credential Rebind Count"
          },
          "callLogId": {
            "type": "string",
            "description": "UUID of the CallLog OpenRegister object created by the underlying CallService::call() dispatch (Decision 2/4), so an operator can pivot from this audit entry to the raw CallLog when diagnosing a failed promotion",
            "title": "Call Log ID"
          },
          "created": {
            "type": "string",
            "format": "date-time",
            "description": "OR-managed creation timestamp",
            "title": "Created At"
          },
          "updated": {
            "type": "string",
            "format": "date-time",
            "description": "OR-managed update timestamp",
            "title": "Updated At"
          }
        },
        "x-openregister-seed": [],
        "x-openregister-archival": {
          "retention": {
            "default": "P90D"
          }
        },
        "appendOnly": true,
        "immutable": true
      }
    },
    "objects": [
      {
        "@self": {
          "register": "integriq",
          "schema": "source",
          "slug": "environment-local-source"
        },
        "name": "Local Environment API",
        "slug": "environment-local-source",
        "description": "Connectivity Source for the 'local' environment: this OpenConnector instance's own API, seeded so a fresh install has at least one usable promotion source environment.",
        "type": "api",
        "isEnabled": true,
        "location": "http://localhost"
      },
      {
        "@self": {
          "register": "integriq",
          "schema": "source",
          "slug": "environment-acceptance-source"
        },
        "name": "Acceptance Environment API",
        "slug": "environment-acceptance-source",
        "description": "Connectivity Source for the 'acceptance' environment: a pre-production OpenConnector instance. Authenticates via a credentialRef placeholder (never an embedded secret); the referenced credential must be created in the OpenRegister credential broker before this Source is used for a real promotion.",
        "type": "api",
        "isEnabled": false,
        "location": "https://acceptance.example.org",
        "configuration": {
          "authentication": {
            "credentialRef": {
              "credentialName": "acceptance-environment-api-key"
            }
          }
        }
      },
      {
        "@self": {
          "register": "integriq",
          "schema": "environment",
          "slug": "local"
        },
        "name": "Local",
        "slug": "local",
        "role": "source",
        "sourceRef": "@ref:source:environment-local-source",
        "description": "This instance"
      },
      {
        "@self": {
          "register": "integriq",
          "schema": "environment",
          "slug": "acceptance"
        },
        "name": "Acceptance",
        "slug": "acceptance",
        "role": "target",
        "sourceRef": "@ref:source:environment-acceptance-source",
        "description": "Acceptance environment for pre-production promotion"
      }
    ]
  }
}
