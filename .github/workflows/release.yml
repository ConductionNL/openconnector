name: Release

on:
  push:
    branches: [main, beta, development]

jobs:
  unstable:
    if: false # unstable builds are disabled
    uses: ConductionNL/.github/.github/workflows/release.yml@main
    with:
      release-type: unstable
      app-name: openconnector
      verify-vendor-deps: false
      vendor-check-paths: ''
    secrets: inherit

  beta:
    if: github.ref == 'refs/heads/beta'
    uses: ConductionNL/.github/.github/workflows/release.yml@main
    with:
      release-type: beta
      app-name: openconnector
      verify-vendor-deps: false
      vendor-check-paths: ''
    secrets: inherit

  stable:
    if: github.ref == 'refs/heads/main'
    uses: ConductionNL/.github/.github/workflows/release.yml@main
    with:
      release-type: stable
      app-name: openconnector
      verify-vendor-deps: false
      vendor-check-paths: ''
    secrets: inherit
